# 桌面安装包全链重打记录(2026-08-13)

主线当日并入大量前后端新功能(LLM 配置闭环、运行队列、doctor 体检、诊断包导出、
CSP 调整、九面板前端改造等),冻结产物与安装包过期 —— 本轮在独立 worktree
(基点 `19d59b1`)全链重打并完成装/卸验证。流程与断言矩阵沿用
`docs/RELEASE-CHECKLIST.md` §5(2026-08-12 首次全量打包校准版)。

## 1. 版本与工具链

| 项 | 值 |
|---|---|
| 安装包版本 | **0.1.1**(`tauri.conf.json` + `Cargo.toml` 同步递增一个 patch 位) |
| 后端版本 | 0.1.0(`pyproject.toml` 归主线管,不在本轮所有权内 —— `/api/health`、`/api/version` 报 0.1.0,与安装包 0.1.1 存在已知偏斜,下轮主线统一版本时消除) |
| Python / PyInstaller | 3.14.3 / 6.22.0(钉住,`requirements-desktop.txt`) |
| Rust / tauri-cli | cargo 1.96.0 / tauri-cli 2.11.4(官方预编译包,SHA256 与脚本 `$KnownSha256` 匹配) |
| NSIS | 3.11(`%LOCALAPPDATA%\tauri\NSIS` 既有缓存,离线可用) |
| 构建机 | Win 10.0.26200;`CARGO_TARGET_DIR=E:\cargo-target-desktop-bundle` |

## 2. 依赖核对结论(本轮任务 ①)

- **brain/llm 新代码纯 stdlib**(`json/os/urllib/pathlib/dataclasses/re`),AST 全量扫描
  `src/insar_agent` 的第三方 import 面 = fastapi/uvicorn/starlette/pydantic、yaml、
  numpy/h5py、httpx(version_router 可选消费,原已随包)、mcp(仅 `mcp/server.py`)——
  **`requirements.txt` 无缺口,零改动**;pyproject 主依赖(fastapi/uvicorn/pyyaml)全覆盖。
- **mcp 可选组不进冻结包**:构建 venv 未装 mcp SDK,实测 dist `_internal/` 无 mcp 条目。
  同时给 spec 上了双保险(见 §3),使结论不再依赖「venv 恰好没装」。

## 3. 本轮代码改动(全部在 desktop/**)

| 文件 | 改动 |
|---|---|
| `backend-bundle/insar_backend.spec` | ① `collect_submodules("insar_agent")` 增加 filter 过滤 `insar_agent.mcp*`(MCP 是可选组 [mcp] 的独立 stdio 入口,后端 app 不 import 它);② `excludes` 增 `mcp`、`insar_agent.mcp` —— 冻结包内容不随构建 venv 是否装了 mcp SDK 漂移 |
| `tauri.conf.json` | version 0.1.0 → 0.1.1 |
| `Cargo.toml` / `Cargo.lock` | 壳 crate 版本同步 0.1.1 |

## 4. 构建产物清单

| 产物 | 大小 | 位置 |
|---|---|---|
| NSIS 安装包 `InSAR-Agent_0.1.1_x64-setup.exe` | 30,654,847 B(29.2 MB) | `E:\cargo-target-desktop-bundle\release\bundle\nsis\` |
| 冻结后端 onedir `insar-backend\` | 246 文件,74,568,299 B(71.1 MB) | `desktop\backend-bundle\dist\`(已打进安装包 `backend\`) |
| 壳裸 exe `insar-agent-desktop.exe` | 14.8 MB | `E:\cargo-target-desktop-bundle\release\` |
| 装后体积 | 86.3 MB | — |

产物均不入库(.gitignore 口径不变)。耗时:后端冻结 193s(全新 worktree venv 含装依赖)、
cargo check 163s、cargo test 141s、cargo release 115s、tauri build 183s。
上一版(2026-08-12,0.1.0)对照:后端 onedir 70.3 → 71.1 MB,安装包 28.9 → 29.2 MB
(主线新增代码所致,量级合理)。

## 5. 验证记录

### 5.1 冻结产物契约门禁(tests/test_frozen_probe.py,零改动只跑)

`6 passed, 1 xfailed in 27.56s` —— 全绿。xfail 为已登记的已知缺陷
(`probe.py` 模块级 `Path.home()`,家目录环境变量全缺时启动失败),按设计预期失败。
纯净度断言过:dist 无 hypothesis/ruff/pytest/PIL/pyinstaller 混入;数据文件
(schema.sql / contract.yaml / wsl_wrapper.sh / local_wrapper.py / 4 个 scenario_packs)齐全。

### 5.2 冻结产物功能冒烟(dist 直启,随机高位端口 48096 + 一次性 INSAR_HOME)

| 检查 | 结果 |
|---|---|
| `/api/health` | 200 `{"ok":true,"version":"0.1.0"}`(启动约 5s) |
| `/api/setup/status` | 200,8 项 checks,engines 字典在(首呼 20.4s,WSL 冷探测,符合预期) |
| `/api/llm/config` | 200 `configured:false, source:none, api_key_masked:""` —— 干净目录无密钥,正确 |
| `/`(静态 UI) | 200,含当日新增接线 `js/llmsettings.js` 与 `js/tour.js` |
| 收尾 | 进程杀净,端口释放,临时目录删除 |

### 5.3 壳测试与安装验证(断言矩阵沿用 RELEASE-CHECKLIST §5.3)

壳单测 `cargo test`:45 passed / 0 failed。

| 环节 | 断言 | 实测 |
|---|---|---|
| 静默安装 `/S /D=<临时目录>` | 退出码 0,布局 `{insar-agent-desktop.exe, backend\insar-backend.exe, uninstall.exe}` | ✓ |
| 注册表 | `HKCU\...\Uninstall\InSAR-Agent` DisplayVersion=**0.1.1**、InstallLocation 正确 | ✓ |
| 快捷方式 | 开始菜单 + 桌面(静默装自动建,NSIS 模板行为) | ✓ |
| 启动 | 随机高位端口(48878,**未触碰 8873**)`/api/health` 200 约 5s;主窗题 InSAR-Agent | ✓ |
| 冻结后端优先 | sidecar 日志首行 `spawn sidecar:冻结后端:<安装目录>\backend\insar-backend.exe` | ✓ |
| 冻结形态 | `/api/version` 报 `build.frozen:true`,python 3.14.3 | ✓ |
| 静默卸载 `/S` | 退出码 0;安装目录、两处快捷方式、注册表项全清;残留进程 0 | ✓ |
| 数据不误删 | `%LOCALAPPDATA%\insar-agent-data` 时间戳全为 8/12(上轮验证遗留,本轮 INSAR_HOME 重定向未新写);`%LOCALAPPDATA%\dev.insar.agent`(窗口状态)保留 —— 均为「卸载不删用户数据」设计语义 | ✓ |

## 6. 过程备忘(下次打包直接抄)

- `scripts/fetch_tauri_cli.ps1` 直连 GitHub 再次涓流卡死(已知坑,CHECKLIST §5.4-4),
  按 §5.1-② 的 curl 兜底一次成功:`ghfast.top` 镜像 19s 下完,SHA256 与脚本内
  `$KnownSha256['2.11.4|x86_64-pc-windows-msvc']` 匹配后解压 `.tools\tauri-cli\`。
- 构建顺序铁律仍成立:后端冻结必须先于 cargo(`bundle.resources` 源目录缺失时
  tauri-build 编译期直接失败)。
- PyInstaller 唯一告警 `Hidden import "tzdata" not found` 为历史既有(zoneinfo 的
  Windows 回退包,未声明依赖),与本轮改动无关,运行不受影响。
- tauri-build 会重写 `gen/schemas/*` 与 `permissions/autogenerated/*` 的行尾
  (LF,内容零变化),提交前 `git checkout` 还原即可,不必入库。
