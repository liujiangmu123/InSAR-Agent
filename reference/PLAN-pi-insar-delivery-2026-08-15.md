# pi-insar 设计完备性分析 + 文件级交付计划(2026-08-15)

> **本文档已迁移。唯一有效版本在:[`pi-insar/docs/plan/`](../pi-insar/docs/plan/00-README.md)。**
> 迁移原因:计划与被开发的 `pi-insar/` 同居一处,执行 AI 打开 `pi-insar/` 即可开发;pi 上游源码快照也在 `pi-insar/reference/pi/`。

## 执行 AI 入口

打开 [`pi-insar/docs/plan/00-README.md`](../pi-insar/docs/plan/00-README.md)(总索引):

- 背景阅读:`01`(约定)→ `02`(仓库地图)→ `03`(设计分析)→ `04`(科学能力地图)→ `05`(后端总账)→ `06`(完备性判定);
- 执行 Phase:`10-13`(基线)→ `20-24`(工具面 16→31)→ `30-35`(科学能力扩展)→ `40`(收口);
- 附录:`41`(最终验收清单)、`42`(坑表)、`43`(pi 升级)、`44`(术语)。

本目录(`reference/`)不再保存计划正文,此文件仅作历史指针。
