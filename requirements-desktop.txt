# 桌面打包(desktop/backend-bundle)专用依赖 —— 运行时依赖见 requirements.txt
# 安装:.venv\Scripts\python.exe -m pip install -r requirements-desktop.txt
# 版本钉住:PyInstaller 跨版本的 bootloader/布局行为差异大,升级须重跑冒烟验证
pyinstaller==6.22.0
